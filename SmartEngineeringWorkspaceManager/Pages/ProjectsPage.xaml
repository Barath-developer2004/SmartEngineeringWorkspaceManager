<UserControl x:Class="SmartEngineeringWorkspaceManager.Pages.ProjectsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" 
             Style="{StaticResource FadeInPageStyle}"
             Background="{DynamicResource MainBg}">

    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Left Panel for adding/editing projects -->
            <ColumnDefinition Width="340"/>
            <!-- Right Panel for list -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Form Panel -->
        <Border Grid.Column="0" Style="{StaticResource StandardCardStyle}" Margin="12,12,0,12">
            <StackPanel>
                <TextBlock x:Name="FormTitle" Text="Create New Project" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,24"/>

                <TextBlock Text="Project Name *" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,4"/>
                <TextBox x:Name="txtProjectName"/>

                <TextBlock Text="Deadline *" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,4"/>
                <DatePicker x:Name="dpDeadline" Margin="0,4,0,16"/>

                <TextBlock Text="Status" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,4"/>
                <ComboBox x:Name="cbStatus">
                    <ComboBoxItem Content="Active" IsSelected="True"/>
                    <ComboBoxItem Content="Planning"/>
                    <ComboBoxItem Content="Completed"/>
                    <ComboBoxItem Content="On Hold"/>
                </ComboBox>

                <TextBlock Text="Description" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,4"/>
                <TextBox x:Name="txtDescription" Height="100" TextWrapping="Wrap" AcceptsReturn="True"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button x:Name="btnClear" Content="Clear" Style="{StaticResource PrimaryButtonStyle}" Background="Transparent" Foreground="{DynamicResource TextSecondary}" Margin="0,0,10,0" Click="BtnClear_Click"/>
                    <Button x:Name="btnSave" Content="Save Project" Style="{StaticResource PrimaryButtonStyle}" Click="BtnSave_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Right DataGrid Panel -->
        <Border Grid.Column="1" Style="{StaticResource StandardCardStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Search and Action Bar -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Project Directory" Style="{StaticResource HeaderTextStyle}" Margin="0"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBox x:Name="txtSearch" Width="220" Text="Search projects..." Foreground="{DynamicResource TextSecondary}" GotFocus="TxtSearch_GotFocus" LostFocus="TxtSearch_LostFocus" TextChanged="TxtSearch_TextChanged" Margin="0"/>
                    </StackPanel>
                </Grid>

                <!-- DataGrid to show projects -->
                <DataGrid x:Name="dgProjects" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionChanged="DgProjects_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding ProjectId}" Width="50"/>
                        <DataGridTextColumn Header="Project Name" Binding="{Binding ProjectName}" Width="*"/>
                        <DataGridTextColumn Header="Deadline" Binding="{Binding Deadline, StringFormat=d}" Width="100"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>

                        <!-- Action Column for Delete button -->
                        <DataGridTemplateColumn Header="Actions" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Delete" Style="{StaticResource PrimaryButtonStyle}" Background="{DynamicResource DangerRed}" Padding="10,6" Click="BtnDelete_Click"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>