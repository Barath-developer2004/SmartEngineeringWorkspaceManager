<UserControl x:Class="SmartEngineeringWorkspaceManager.Pages.DocumentsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900" 
             Style="{StaticResource FadeInPageStyle}"
             Background="{DynamicResource MainBg}">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header / Control Bar -->
        <Border Grid.Row="0" Style="{StaticResource StandardCardStyle}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Document Vault" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Center" Margin="0"/>

                <WrapPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBox x:Name="txtSearch" Width="220" Text="Search documents..." Foreground="{DynamicResource TextSecondary}" GotFocus="TxtSearch_GotFocus" LostFocus="TxtSearch_LostFocus" TextChanged="TxtSearch_TextChanged" Margin="0,0,16,0" VerticalAlignment="Center"/>

                    <TextBlock Text="Assign to Project:" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <ComboBox x:Name="cbProjects" Width="200" DisplayMemberPath="ProjectName" SelectedValuePath="ProjectId" VerticalAlignment="Center" Margin="0,0,16,0"/>

                    <Button Content="+ Upload Document" Style="{StaticResource PrimaryButtonStyle}" Click="BtnUpload_Click" VerticalAlignment="Center"/>
                </WrapPanel>
            </Grid>
        </Border>

        <!-- DataGrid to show documents -->
        <Border Grid.Row="1" Style="{StaticResource StandardCardStyle}" Margin="0">
            <DataGrid x:Name="dgDocuments" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Doc ID" Binding="{Binding DocumentId}" Width="80"/>
                    <DataGridTextColumn Header="File Name" Binding="{Binding FileName}" Width="*"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding FileType}" Width="100"/>
                    <DataGridTextColumn Header="Related Project" Binding="{Binding ProjectName}" Width="200"/>
                    <DataGridTextColumn Header="Date Uploaded" Binding="{Binding UploadDate, StringFormat=g}" Width="150"/>

                    <!-- Actions Column -->
                    <DataGridTemplateColumn Header="Actions" Width="310">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Open Base" Style="{StaticResource PrimaryButtonStyle}" Padding="10,6" Margin="0,0,5,0" Click="BtnOpen_Click"/>
                                    <Button Content="View Revisions" Style="{StaticResource PrimaryButtonStyle}" Background="{DynamicResource BrandOrange}" Padding="10,6" Margin="0,0,5,0" Click="BtnViewRevisions_Click"/>
                                    <Button Content="Delete" Style="{StaticResource PrimaryButtonStyle}" Background="{DynamicResource DangerRed}" Padding="10,6" Click="BtnDelete_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Revision History Pop-out Panel (Hidden by default) -->
        <Border x:Name="RevisionPanel" Grid.Row="1" Style="{StaticResource StandardCardStyle}" Margin="0" Visibility="Collapsed" Panel.ZIndex="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock x:Name="RevTitleText" Text="Revision History" Style="{StaticResource HeaderTextStyle}" Margin="0"/>
                    <Button Content="Close" HorizontalAlignment="Right" Background="Transparent" Foreground="{DynamicResource DangerRed}" BorderThickness="0" FontSize="14" FontWeight="Bold" Cursor="Hand" Click="BtnCloseRevisions_Click"/>
                </Grid>

                <DataGrid x:Name="dgRevisions" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Version" Binding="{Binding VersionNumber}" Width="100" FontWeight="Bold"/>
                        <DataGridTextColumn Header="Modified Date" Binding="{Binding ModifiedDate, StringFormat=g}" Width="160"/>
                        <DataGridTextColumn Header="Modifier" Binding="{Binding ModifierName}" Width="160"/>
                        <DataGridTextColumn Header="Comment" Binding="{Binding RevisionComment}" Width="*"/>

                        <DataGridTemplateColumn Header="Open" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Open File" Style="{StaticResource PrimaryButtonStyle}" Padding="12,6" Click="BtnOpenRevision_Click"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Add New Revision Form -->
                <StackPanel Grid.Row="2" Margin="0,24,0,0">
                    <TextBlock Text="Upload New Revision" Style="{StaticResource SubHeaderTextStyle}"/>
                    <Grid Margin="0,8,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0" Text="Version: " VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox x:Name="txtNewVersion" Grid.Column="1" HorizontalAlignment="Stretch" Margin="0"/>

                        <TextBlock Grid.Column="2" Text="Comment: " VerticalAlignment="Center" Margin="20,0,10,0"/>
                        <TextBox x:Name="txtRevComment" Grid.Column="3" HorizontalAlignment="Stretch" Margin="0"/>

                        <Button Grid.Column="4" Content="Browse &amp; Upload" Style="{StaticResource PrimaryButtonStyle}" Background="{DynamicResource BrandOrange}" Margin="15,0,0,0" Click="BtnUploadRevision_Click"/>
                    </Grid>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>