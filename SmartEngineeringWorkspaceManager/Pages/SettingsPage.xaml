<UserControl x:Class="SmartEngineeringWorkspaceManager.Pages.SettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Style="{StaticResource FadeInPageStyle}"
             Background="{DynamicResource MainBg}">
    <Grid>
        <Border Style="{StaticResource StandardCardStyle}" VerticalAlignment="Top" HorizontalAlignment="Left" Width="400">
            <StackPanel>
                <TextBlock Style="{StaticResource HeaderTextStyle}" Text="Appearance Settings"/>

                <TextBlock Text="Select Application Theme:" Style="{StaticResource SubHeaderTextStyle}" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,25">
                    <Button Content="Light Mode" Style="{StaticResource PrimaryButtonStyle}" Click="LightTheme_Click" Margin="0,0,20,0" Width="120" Background="{DynamicResource BrandOrange}"/>
                    <Button Content="Dark Mode" Style="{StaticResource PrimaryButtonStyle}" Click="DarkTheme_Click" Width="120" Background="#333333"/>
                </StackPanel>

                <TextBlock Text="Other Settings Coming Soon" Foreground="{DynamicResource TextSecondary}" FontStyle="Italic"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>